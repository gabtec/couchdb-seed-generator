const generateBasicSetup = require("./generateBasicSetup");
const generateSetupFromInstance = require("./generateSetupFromInstance");
const makeUser = require('./makeUser');
const makeDeploy = require('./makeDeploy');

module.exports = {
  generateBasicSetup,
  generateSetupFromInstance,
  makeUser,
  makeDeploy,
};
