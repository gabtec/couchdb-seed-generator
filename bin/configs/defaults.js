module.exports = {
  url: "http://admin:admin@localhost:5984",
  outputFolder: "./couchdb",  // where to write output files
  deleteFolder: false,        // delete existing outputFolder, before new generation = false
  // exclude: [],                // list of db names to exclude (only for remote mode)
  // include: [],                // list of db names to exclude (only for remote mode)
  verbose: false,
};
